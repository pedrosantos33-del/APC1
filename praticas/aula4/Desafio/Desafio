#include <stdio.h>

int main() {
    int x = 5 / 2;   // x = 2
    int y = x++;     // y = 2, depois x = 3
    int z = x % 2;   // z = 3 % 2 = 1
    printf("x = %d, y = %d, z = %d\n", x, y, z);
    return 0;
}